# Sao chép file này thành .env (cùng thư mục cp2/) rồi điền key thật.
# .env đã được ignore ở .gitignore gốc (*.env) — không bao giờ commit file .env thật.
# Lấy key miễn phí tại Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=
